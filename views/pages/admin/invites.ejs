<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../../partials/head.ejs") %>
    <title>Umfrage - <%= survey.name %> - Einladungen</title>
  </head>

  <body>
    <header>
      <%- include("../../partials/header.ejs") %>
    </header>
    <div class="container">
      <h1><%= survey.name %></h1>
      <h3><%= survey.description %></h3>
      <br />
      <h4>Einladungen</h4>
      <form action="" method="POST" class="my-4 form-inline">
        <label for="text" class="sr-only">Text</label>
        <input
          required
          placeholder="MaxMusterman@Muster.de"
          type="email"
          name="email"
          id="email"
          class="form-control col mr-2"
        />
        <button
          class="btn btn-success"
          type="submit"
          name="action"
          value="invite"
        >
          Einladen
        </button>
      </form>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>ID</th>
            <th>Ablaufdatum</th>
            <th>
              <form action="" method="POST">
                <button type="submit" name="action" value="deleteAll">
                  alle löschen
                </button>
              </form>
            </th>
          </tr>
        </thead>
        <tbody>
          <% invites.forEach(invite => { %>
          <tr>
            <td>
              <a><%= invite._id %></a>
            </td>
            <td>
              <a><%= invite.expiryDate %></a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <h4>
        Eingeladenene E-Mail Adressen: (<%= survey.invitedEmails.length %>)
      </h4>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Email</th>
          </tr>
        </thead>
        <tbody>
          <% survey.invitedEmails.forEach(email => { %>
          <tr>
            <td>
              <a><%= email %></a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <a href="/admin/survey/<%= survey._id %> ">Zurück</a>
    </div>

    <footer>
      <%- include("../../partials/footer.ejs") %>
    </footer>
  </body>
</html>
